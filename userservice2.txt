hello telecom provider
hello software provider
git init → initializes Git
git add . → adds all files
git commit -m → creates commit
git remote add origin → connects local project to GitHub repo
git push → uploads code to GitHub

to add human avtar in this file.